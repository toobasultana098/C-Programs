#include<stdio.h>
#include<string.h>
int main()
{
    char str1[15];
    char str2[15];
    strcpy(str1, "Finally");
    puts(str1);
    strcpy(str2, "Mid-sem is over !");
    puts(str2);
    return (0);
}
